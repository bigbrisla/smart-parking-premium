# Copy to .env and fill in. The .env file must NOT be committed.

# Telegram bot token (from @BotFather). Needed to run the Telegram bot.
TELEGRAM_TOKEN=

# URL of the edge instance the bot connects to by default (Gran Reno).
EDGE_URL=http://127.0.0.1:8001
